## IDF Component Manager Manifest File
dependencies:
  espressif/esp_tinyusb: ^1
  lvgl/lvgl: ^8.4.0
  espressif/esp_sccb_intf: ^0.0.3
  espressif/esp_video: ^0.5.1
  espressif/esp_cam_sensor: ^0.5.2
  espressif/es8311: ^1.0.0
  espressif/esp_codec_dev: ^1.3.4
  espressif/esp_hosted: '*'
  espressif/esp_wifi_remote: 0.15.0
  espressif/usb_host_msc: ^1.1.2
  espressif/esp_lcd_st7796: ^1.3.5
  espressif/esp-sr: ^2.0.0
  espressif/led_strip: ^2.5.0

  ## Required IDF version
  idf:
    version: '>=4.1.0'
  # # Put list of dependencies here
  # # For components maintained by Espressif:
  # component: "~1.0.0"
  # # For 3rd party components:
  # username/component: ">=1.0.0,<2.0.0"
  # username2/component2:
  #   version: "~1.0.0"
  #   # For transient dependencies `public` flag can be set.
  #   # `public` flag doesn't have an effect dependencies of the `main` component.
  #   # All dependencies of `main` are public by default.
  #   public: true
  espressif/esp-tflite-micro: ^1.3.5
  esp-idf-lib/dht: ^1.2.0
